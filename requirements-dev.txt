# Light test environment. No GPU, model, or dataset dependency: the suite runs
# on tiny temporary fixtures. Bounds match requirements.txt where they overlap.
pytest>=8,<9
nbformat>=5,<6

# Imported by the utilities the suite exercises.
numpy>=1.23.5,<2.0.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.5.0
pillow>=9.0.0
ipython>=7.0.0
